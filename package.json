{
  "name": "neo-js-examples",
  "license": "MIT",
  "scripts": {
    "rebuild": "rm -rf ./node_modules && npm install",
    "rebuild:hard": "rm -f ./package-lock.json && npm run rebuild",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@cityofzion/neo-js": "0.13.0",
    "lodash": "^4.17.11",
    "moment": "^2.22.2"
  },
  "devDependencies": {}
}
